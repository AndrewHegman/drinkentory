import { makeStyles } from "@material-ui/styles";

export const useStylesChartStyles = makeStyles({
  favoritesToggleContainer: {
    display: "flex",
    marginTop: "15px",
    position: "relative",
    left: "25%",
  },
  favoritesToggleLabel: {
    marginLeft: "12px",
    alignSelf: "center",
  },
});
