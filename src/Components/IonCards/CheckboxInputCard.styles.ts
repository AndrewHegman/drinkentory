import { makeStyles } from "@material-ui/styles";

export const useCheckboxInputCardStyles = makeStyles({
  input: {
    "--padding-top": "0px",
    fontSize: 28,
    color: "black",
    paddingTop: 0,
  },
});
