import { makeStyles } from "@material-ui/styles";
import { greyFont } from "../../Utils/Constants";

export const useCommonIonCardStyles = makeStyles({
  header: {
    fontSize: 18,
    fontWeight: "normal",
    color: greyFont,
  },
});
