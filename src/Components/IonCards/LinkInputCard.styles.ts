import { makeStyles } from "@material-ui/styles";

export const useLinkInputCardStyles = makeStyles({
  link: {
    textDecoration: "none",
  },

  content: {
    fontSize: 28,
    color: "black",
    paddingTop: 0,
  },
});
