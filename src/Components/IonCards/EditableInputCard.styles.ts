import { makeStyles } from "@material-ui/styles";

export const useEditableInputCardStyles = makeStyles({
  input: {
    "--padding-top": "0px",
    fontSize: 28,
    color: "black",
    paddingTop: 0,
  },
});
